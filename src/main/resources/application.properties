spring.application.name=CRUDOperation
server.port= 9393
spring.datasource.url = jdbc:mysql://localhost:3306/productdatabase?useSSL=true&requireSSL=true
spring.datasource.username = root
spring.datasource.password = Raja@0904

# JPA Settings
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.profiles.active = dev

name : local

available.product = pen,paper,book

management.endpoints.web.exposure.include= *,shutdown
#management.endpoints.web.exposure.include= info,health,mappings,shutdown

#management.endpoints.web.exposure.exclude= mappings
management.endpoint.shutdown.enabled = true
